{
  "name": "react-form-group",
  "version": "2022.04.28.22.11.04",
  "description": "Lib for form state management",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf lib && tsc && cp package.json lib",
    "dist": "yarn build && cp -r src lib && DIST=$(./dist.py) && echo $DIST && cp -r lib $DIST",
    "build:link": "yarn build && rm -rf react-form-group && cp -r lib react-form-group && cp -r src react-form-group/src",
    "test": "ts-jest tests"
  },
  "dependencies": {
    "axios": "~0.27.2",
    "lodash": "~4.17.21",
    "react": "~17.0.2",
    "immutable": "4.0.0"
  },
  "peerDependencies": {
  },
  "devDependencies": {
    "@types/react": "~18.0.9",
    "@types/lodash": "~4.14.182",
    "@types/jest": "~27.5.1",
    "jest": "~28.1.0",
    "ts-jest": "~28.0.2",
    "typescript": "~4.6.4"
  },
  "types": "lib/index.d.ts",
  "files": [
    "lib/",
    "index.ts"
  ]
}
